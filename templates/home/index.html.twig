{% extends 'base.html.twig' %}

{% block title %}HomePage{% endblock %}

{% block body %}

    <div class="home-bg bg">
        <a href="#tricks" class="home-link-top" title="Aller en bas de la page">
            <i class="fas fa-chevron-down fa-3x chevron-down"></i>
        </a>
    </div>

    <div class="container">
        <h1 class="h3 text-center">Liste des tricks</h1>

        <div id="tricks" class="row">
            {% for trick in tricks %}
                {% include 'tricks/_tricks.html.twig' %}
            {% endfor %}
        </div>
        <a href="#tricks" class="home-link-bottom" title="Aller en haut de la page">
            <i class="fas fa-chevron-up fa-3x chevron-up"></i>
        </a>

    </div>

{# ajouter load more#}
{% endblock %}
