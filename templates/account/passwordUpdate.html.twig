{% extends "base.html.twig" %}


{% block title %}Modifier le mot de passe{% endblock %}

{% block body %}
    <div class="container text-center">
        <h1 class="h3">Modifier mon mot de passe</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-success">Enregistrer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}